import { SpinnerContainer, LoadingSpinner } from "./Spinner.styled"

SpinnerContainer
const Spinner = () => {
    return (
      <SpinnerContainer>
          <LoadingSpinner></LoadingSpinner>
      </SpinnerContainer>
    )
  }
  export default Spinner